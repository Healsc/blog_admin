import Vue from 'vue'
import { Button ,Row, Menu, Submenu,MenuItem, Col
,MenuItemGroup, Input, Form, FormItem, RadioGroup,RadioButton ,Select,
Option,Message} from 'element-ui'

Vue.use(Button).use(Row).use(Menu).use(Submenu)
.use(MenuItem).use(Col).use(MenuItemGroup).use(Input)
.use(Form).use(FormItem).use(RadioGroup).use(RadioButton)
.use(Select).use(Option).use(Message)
